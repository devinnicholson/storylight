steps:
  - name: gcr.io/cloud-builders/docker
    args: [build, -t, '${_IMAGE}', .]
  - name: gcr.io/cloud-builders/docker
    args: [push, '${_IMAGE}']
images: ['${_IMAGE}']
timeout: 2400s
options:
  machineType: E2_HIGHCPU_8
  diskSizeGb: 500
